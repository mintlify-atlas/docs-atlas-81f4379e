---
title: "DeadPunk OS: A Fake Browser-Based Operating System"
sidebarTitle: "DeadPunk OS"
description: "DeadPunk OS is a browser-based fake OS experience — the avant-garde public face of MaRCoRe's project hub. It's not broken, it's art."
---

DeadPunk OS is a browser-based fake operating system that serves as the public face of the XxYouDeaDPunKxX project hub. It lives at the GitHub Pages URL for MaRCoRe's profile repository, and it is the first thing you encounter when you show up at the hub looking for something real. Is it a practical tool? No. Is it broken? Also no. It's avant-garde. There is a difference, and DeadPunk OS would like you to appreciate it.

## What It Is

DeadPunk OS is not a real operating system. It does not run processes, manage memory, or have a kernel. What it is, is a browser-based interactive experience styled to look and feel like a desktop OS — boot sequence, terminal aesthetic, fake modules, questionable system status messages, and all.

It was built as a GitHub Pages deployment for the `XxYouDeaDPunKxX` profile repository. Its job is to be the weird, honest front door to a collection of projects that don't fit neatly into a portfolio template. The README puts it plainly: these projects come from real use, not from trying to look impressive. DeadPunk OS reflects that.

The boot sequence tells you what you need to know about the aesthetic philosophy:

```
[OK]    profile loaded, against all odds
[OK]    modules loaded? no questions please
[?]     runtime making noises, nice
[WARN]  template tried something, failed
[OOPSIE] structural integrity compromised
[DUCT]  tape logic doing its best
[BOOT]  booting, thank god
```

If you expected a polished landing page, the OS has noted your expectation and is choosing to ignore it.

## What You Can Do With It

<CardGroup cols={2}>
  <Card title="Explore the Hub" icon="compass">
    DeadPunk OS surfaces links to the GitHub profile and project repositories. Navigate to the project index from inside the OS interface or use the raw manifest links if JavaScript gives up on you.
  </Card>
  <Card title="Read the Raw Index" icon="file-lines">
    The OS exposes raw bus links — `llms.txt`, `robots.txt`, `sitemap.xml`, and `raw-manifest.json` — for clients (human or AI) that want the structured project index without booting the fake OS.
  </Card>
  <Card title="Boot the Experience" icon="power-off">
    Watch the fake boot sequence, interact with the module interface, and experience the DeadPunk OS aesthetic. It is a thing you experience, not a thing you use to get things done.
  </Card>
  <Card title="Inspect the Modules" icon="puzzle-piece">
    The OS has a module system. Modules load (or don't), status indicators report (mostly bad news), and the whole thing coheres into a working fake desktop. Pick a failure mode and inspect the module.
  </Card>
</CardGroup>

## The Aesthetic

DeadPunk OS is the public expression of a specific design stance: build for real use, not for appearances. The projects in the MaRCoRe hub are practical AI tools, behavioral protocols, and workflow systems built to solve actual problems. They are not portfolio pieces.

DeadPunk OS applies that same stance to the hub itself. Instead of clean cards, gradient backgrounds, and confidence-inspiring copy about scalable solutions, you get a fake OS with a broken structural integrity warning and duct tape logic doing its best.

The aesthetic is technical, deliberately rough, and indifferent to what looks impressive. Status is bad. The runtime is making noises. The template tried something and failed. Everything is fine.

Build — pure spite & prompts.  
Stack — held by duct tape under threat.  
Mood — weird ideas, real problems.

That is the vibe. The projects are serious. The landing page is not trying to sell you anything.

## Visiting DeadPunk OS

The OS is live at its GitHub Pages URL. Open it in a browser and let it boot:

**[xxyoudeadpunkxx.github.io/XxYouDeaDPunKxX/](https://xxyoudeadpunkxx.github.io/XxYouDeaDPunKxX/)**

If JavaScript is disabled or decides to opt out, the raw bus links at the top of the page (`llms.txt`, `raw-manifest.json`) give you a plain-text project index.

The source for DeadPunk OS lives in the `DeadPunk.OS_0.WTH` branch of the [XxYouDeaDPunKxX profile repository](https://github.com/XxYouDeaDPunKxX/XxYouDeaDPunKxX/tree/DeadPunk.OS_0.WTH).

<Note>
DeadPunk OS is a creative experiment and an aesthetic statement — not a practical tool. It is the front door to the hub, not a project you integrate into a workflow. The actual tools in the MaRCoRe hub are the projects it links to, including the [ChatGPT Local Agent MCP](/integrations/chatgpt-local-agent-mcp) and the [Cloudflare R2 Remote MCP Worker](/integrations/cloudflare-r2-mcp). DeadPunk OS just has better vibes.
</Note>
